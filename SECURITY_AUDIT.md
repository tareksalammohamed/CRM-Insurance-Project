# تقرير التدقيق الأمني — CRM Insurance

## نطاق التدقيق

تمت مراجعة كود الواجهة، مسارات المصادقة، Edge Functions، سياسات RLS وmigrations، التخزين المحلي وعمليات offline، مسارات الطباعة والتصدير، استيراد الملفات، بوابة الذكاء الاصطناعي، إعدادات Vercel، والتبعيات المباشرة وغير المباشرة.

## الاستثناء المطلوب من صاحب النظام

تم الإبقاء على سلوك إنشاء المستخدم الجديد بكلمة السر الثابتة `123456`، مع تثبيت هذا السلوك على الخادم نفسه حتى لا يستطيع استدعاء الدالة مباشرة تغيير كلمة السر الابتدائية. يظل المستخدم الجديد مسؤولًا عن تغيير كلمة السر بعد تسجيل الدخول.

## الإصلاحات المنفذة

| المجال | النتيجة |
|---|---|
| تصعيد الصلاحيات | إضافة trigger في قاعدة البيانات يمنع المستخدم من تعديل دوره أو مديره أو حالته أو بريده بنفسه عبر تحديث مباشر من المتصفح. |
| الحسابات المعطلة والمحذوفة | منع تحميل ملف الحساب المعطل/المحذوف، ومنع الحسابات غير النشطة من استدعاء الدوال الإدارية أو بوابة AI. |
| العمليات الأوفلاين | مسح طابور العمليات الحساسة عند تسجيل الخروج؛ الطابور قد يحتوي على بيانات عملاء ووثائق. |
| HTML Injection | تهريب بيانات العملاء والوثائق وعنوان نافذة الطباعة قبل إدخالها في `document.write`. |
| بوابة AI | إضافة فحص المستخدم النشط، حد حجم الطلب، حدود عدد الرسائل والأجزاء والصور والنصوص والـtokens، تقييد الحرارة، منع روابط الصور الخارجية، وحد مؤقت للطلبات لكل مستخدم. |
| سياسات قاعدة البيانات | إضافة migration لتثبيت `search_path` للدوال الحساسة ومنع إنشاء إشعارات مزيفة من عميل عادي. |
| Headers | إضافة HSTS وCSP وCORP وX-Permitted-Cross-Domain-Policies، مع الإبقاء على X-Frame-Options وnosniff وReferrer-Policy. |
| PDF | تحديث `jspdf` إلى `4.2.1` ورفع `DOMPurify` عبر npm override إلى إصدار إصلاح. |
| استيراد الملفات | حد 10MB، حد أقصى للأوراق والصفوف، السماح بامتدادات Excel/CSV فقط في مسار الجداول، ومنع Formula Injection في تقرير الأخطاء. |
| المستودع | إضافة `.gitignore` لمنع رفع `node_modules` و`dist` وملفات البيئة. |

## المخاطر المتبقية أو القرارات المقيدة

| المستوى | الملاحظة | الإجراء |
|---|---|---|
| مرتفع | حزمة `xlsx@0.18.5` لا يتوفر لها إصدار إصلاح منشور في npm لمعالجة Prototype Pollution وReDoS. | تم تقليل الخطر بحدود الحجم والأوراق والصفوف، لكن يظل استبدال المكتبة بمحلل Excel مُصان هو الإجراء الأفضل لاحقًا. |
| متوسط | دالة تسجيل الدخول برقم الهاتف تسمح، بحكم وظيفة الدخول الحالية، بتحويل رقم الهاتف إلى البريد الإلكتروني. | لم تُلغَ حتى لا ينكسر تسجيل الدخول برقم الهاتف؛ يجب إضافة rate limiting على مستوى Supabase/Edge أو بوابة auth لاحقًا. |
| منخفض/دفاعي | Edge Functions ما زالت تستخدم CORS wildcard مع اعتمادها على Bearer token. | لا يوجد مفتاح service role في المتصفح، والتحقق من الجلسة قائم؛ يمكن لاحقًا تقييد Origins المعروفة بعد تثبيت قائمة النطاقات الرسمية. |
| تشغيلي | migration `20260825010000_security_hardening.sql` تحتاج تطبيقًا على مشروع Supabase المرتبط فعليًا حتى تصبح حماية RLS وtrigger فعالة في قاعدة البيانات. | لم يتم تخمين مشروع Supabase؛ المشاريع المتاحة في الاتصال الحالي لا تحمل اسم CRM Insurance بوضوح. |

## نتائج التحقق

- `npm run build`: ناجح.
- `git diff --check`: ناجح.
- `npm audit --omit=dev`: لا توجد ثغرات حرجة أو متوسطة أو منخفضة، وتبقى ثغرة عالية واحدة في `xlsx` لعدم توفر إصدار إصلاح من الناشر.
- `npm run lint`: يحتوي على أخطاء وتحذيرات baseline موجودة مسبقًا في المشروع، ولا ترتبط كلها بتعديلات التدقيق؛ البناء الإنتاجي ناجح.

## الملفات الأساسية المتغيرة

- `supabase/functions/ai-gateway/index.ts`
- `supabase/functions/admin-create-user/index.ts`
- `supabase/functions/admin-update-user/index.ts`
- `supabase/functions/ai-test-connection/index.ts`
- `supabase/migrations/20260825010000_security_hardening.sql`
- `src/hooks/useAuth.tsx`
- `src/lib/offlineQueue.ts`
- `src/lib/htmlEscape.ts`
- `src/pages/DataImport/services/dataImportService.ts`
- `src/pages/Customers/services/customerHelpers.ts`
- `src/pages/Policies/services/policyHelpers.ts`
- `src/pages/OrgFormation/pdfExport.ts`
- `vercel.json`
- `package.json` و`package-lock.json`
